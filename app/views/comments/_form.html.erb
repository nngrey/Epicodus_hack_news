<%= render 'layouts/errors', :object => @comment %>
<%= form_for [@link, @comment] do |f| %>
  <div class="form-group ">
    <%= f.label :description, "Add new comment" %><br>
    <%= f.text_area :description %>
  </div>

  <%= f.submit(:class => "btn btn-primary") %>
<% end %>

