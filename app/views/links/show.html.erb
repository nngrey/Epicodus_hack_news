<% content_for(:title, "Show Link | Hack News") %>

<h1><% @link.name %></h1>

<a href="<%= @link.url %>"><%= @link.name %></a> (link domain)
  <p>point "link added mins ago" | # of comments</p>
<hr>

<%= render 'comments/form' %>
<hr>

<% @link.comments.each do |comment| %>
  <p><%= comment.description %></p>
  <p>point 'comment added mins ago' | </p>
<% end %>





<hr>
<%= link_to "Return Home", links_path, :class => "btn btn-primary" %>
